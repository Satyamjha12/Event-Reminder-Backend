# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=
# Or use MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/event-reminder

# JWT Configuration
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Firebase Admin SDK (if using Firebase Auth)
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

# Web Push Notifications (VAPID Keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=

# CORS Configuration
FRONTEND_URL=
